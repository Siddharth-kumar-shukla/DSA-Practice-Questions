#include<bits/stdc++.h>
using namespace std;

#define speedy ios_base::sync_with_stdio(0);cin.tie(0)



void decode()
{
    speedy;
    int n;
    cin >> n;
    int array[n];
    for(int i = 0; i < n; i++){
        cin >> array[i];
    }
    
}

int32_t main()
{
    vector<int>& array;
    int n = array.size();
    for(int i = 0; i < n; i++){
        int n = *max_element(array.begin(), array.end();
            
    }
    return 0;
}
